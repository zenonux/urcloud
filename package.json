{
  "private": true,
  "workspaces": [
    "packages/*",
    "play",
    "build"
  ],
  "scripts": {
    "dev": "pnpm -C play dev",
    "build": "pnpm run -C internal/build start"
  },
  "dependencies": {
    "@urcloud/components": "workspace:*",
    "@urcloud/utils": "workspace:*"
  },
  "peerDependencies": {
    "vue": "^3.2.0"
  }
}
